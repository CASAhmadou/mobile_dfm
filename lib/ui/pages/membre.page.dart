
import 'package:flutter/material.dart';

class MembrePage extends StatelessWidget{
  MembrePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Membre Page'),
      ),
      body: Center(
        child: Text('Bonjour DAAHIRA FOULKOUL MASHOUN'),
      ),
    );
    throw UnimplementedError();
  }
}